# ETL-процесс для банковских данных

## Описание
Комплексный ETL-процесс для загрузки банковских данных из CSV-файлов в PostgreSQL с автоматическим логированием, обработкой различных форматов данных и поддержкой режима UPSERT.

## Структура проекта
- `create_table.py` - создание схем и таблиц в базе данных
- `load_csv.py` - основной ETL-скрипт с логированием
- `config.py` - конфигурация подключения к БД и ключевые поля таблиц
- `csv_files/` - папка с CSV-файлами для загрузки
- `video_link.txt` - ссылка на демонстрационное видео

## Установка зависимостей
```bash
pip install -r requirements.txt
```

## Запуск
1. Создание таблиц:
```bash
python create_table.py
```

2. Загрузка данных:
```bash
python load_csv.py
```

## Особенности
- Поддержка UPSERT операций для обновления существующих записей
- Логирование всех операций в таблицу `LOGS.etl_logs`
- Автоматическое преобразование дат из CSV
- Полная перезагрузка таблицы `DS.FT_POSTING_F`
```

### 3. `video_link.txt`
```txt:video_link.txt
Ссылка на демонстрационное видео:
[Здесь будет ссылка на твоё видео после записи]

Описание видео:
- Демонстрация создания таблиц
- Показ пустой таблицы DS.ft_balance_f
- Запуск ETL-процесса
- Проверка загруженных данных
- Изменение данных в CSV и повторная загрузка
- Демонстрация обновления записей (UPSERT)
